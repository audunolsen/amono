/** @typedef {import("prettier").Config} Config  */

/**
 * Utility for adding type-hints to js-esm based Prettier config files.
 *
 * @param {Config} config
 */
export function defineConfig(config) {
  return config;
}
